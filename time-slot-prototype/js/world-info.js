const GRAVITY = 0.5;
const frameRate = (1 / 60) * 24;
const canvas = document.getElementById("game");
const canvasDimesions = canvas.getBoundingClientRect();
export { GRAVITY, frameRate, canvasDimesions };
